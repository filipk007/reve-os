# Optional — set to enable API key auth on the webhook
# WEBHOOK_API_KEY=your-secret-key-here

# Server port (default: 3000)
PORT=3000

# No ANTHROPIC_API_KEY needed — uses Claude Code Max subscription via CLI
